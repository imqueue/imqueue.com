---
title: "query.safeSqlSpaceCleanup() function · @imqueue/pg-sequelize"
description: "Collapses whitespace outside quoted literals, so a statement fits on one line."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/pg-sequelize","url":"/api/pg-sequelize/latest/"},{"name":"query","url":"/api/pg-sequelize/latest/pg-sequelize.query/"},{"name":"safeSqlSpaceCleanup","url":"/api/pg-sequelize/latest/pg-sequelize.query.safesqlspacecleanup/"}]
sitemap: false
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# query.safeSqlSpaceCleanup() function

Collapses whitespace outside quoted literals, so a statement fits on one line.

**Signature:**

```typescript
export function safeSqlSpaceCleanup(input: string): string;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

input


</td><td>

string


</td><td>

Statement to normalise.


</td></tr>
</tbody></table>

**Returns:**

string

The statement with structural whitespace collapsed to single spaces.

## Remarks

Walks the string tracking whether a single quote is open, so runs of whitespace inside a literal are preserved and only structural whitespace is collapsed. Quote tracking is a simple toggle: it does not understand escaped or doubled quotes, so a literal containing one can throw the parity off.

