---
title: "IRange interface · @imqueue/pg-sequelize"
description: "The shape every range filter shares: a start and an end."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/pg-sequelize","url":"/api/pg-sequelize/latest/"},{"name":"IRange","url":"/api/pg-sequelize/latest/pg-sequelize.irange/"}]
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# IRange interface

The shape every range filter shares: a start and an end.

**Signature:**

```typescript
export interface IRange 
```

## Remarks

`query.withRangeFilters` recognises a range by exactly these two keys, in either order, on a filter property whose name ends in `Range`<!-- -->. So the property NAMES are the contract — an object with any other key set is left alone and filtered as an ordinary value.

The convention it implements: a caller filters `durationRange` and the helper rewrites it onto the real `duration` column. Sending both `duration` and `durationRange` is an error rather than a merge — see [DateRange](/api/pg-sequelize/latest/pg-sequelize.daterange/) for the worked example.

Deliberately untyped at `any`<!-- -->, so a range of anything Postgres supports can implement it; [DateRange](/api/pg-sequelize/latest/pg-sequelize.daterange/) and [NumericRange](/api/pg-sequelize/latest/pg-sequelize.numericrange/) are the two the package ships.

## Properties

<table><thead><tr><th>

Property


</th><th>

Modifiers


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[end](/api/pg-sequelize/latest/pg-sequelize.irange.end/)


</td><td>


</td><td>

any


</td><td>

Upper bound of the range.


</td></tr>
<tr><td>

[start](/api/pg-sequelize/latest/pg-sequelize.irange.start/)


</td><td>


</td><td>

any


</td><td>

Lower bound of the range.


</td></tr>
</tbody></table>

