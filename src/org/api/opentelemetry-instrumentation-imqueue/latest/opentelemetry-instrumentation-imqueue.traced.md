---
title: "traced() function · @imqueue/opentelemetry-instrumentation-imqueue"
description: "Builds a method decorator that wraps each call to the decorated method in its own span, ending it when the method returns — or when the promise it returned…"
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/opentelemetry-instrumentation-imqueue","url":"/api/opentelemetry-instrumentation-imqueue/latest/"},{"name":"traced","url":"/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.traced/"}]
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# traced() function

Builds a method decorator that wraps each call to the decorated method in its own span, ending it when the method returns — or when the promise it returned settles.

**Signature:**

```typescript
export declare function traced(options?: Partial<TracedOptions>): (target: any, methodName: string | symbol, descriptor: TypedPropertyDescriptor<(...args: any[]) => any>) => void;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

options


</td><td>

Partial&lt;[TracedOptions](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.tracedoptions/)<!-- -->&gt;


</td><td>

_(Optional)_ span kind, extra attributes and tracer name. `kind` defaults to [TraceKind.SERVER](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.tracekind/)<!-- -->; `tracerName` defaults to `'basic'`<!-- -->. Attributes given in `tags` are applied last, so they override the ones set automatically.


</td></tr>
</tbody></table>

**Returns:**

(target: any, methodName: string \| symbol, descriptor: TypedPropertyDescriptor&lt;(...args: any\[\]) =&gt; any&gt;) =&gt; void

a method decorator to apply to the methods you want traced

## Remarks

Use this for work worth seeing in a trace that is not itself an RPC, so the automatic client/server spans do not already cover it: a cache rebuild, a report query, a third-party call.

Async methods are handled: a returned thenable keeps the span open until it settles, so the span duration reflects the real work rather than the time to return a promise. A rejection, or a synchronous throw, records the error on the span, marks it `ERROR`<!-- -->, ends it, and re-throws — the decorator never swallows a failure.

Every span it creates is named `method.call`<!-- -->; the decorated method is identified by the `resource.name` attribute (`ClassName.methodName`<!-- -->), not by the span name. The span is not made the active context, so spans created \*inside\* the method do not nest under it — for nesting, rely on [ImqueueInstrumentation](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.imqueueinstrumentation/)<!-- -->, which does establish context for RPC handlers.

## Example


```typescript
import { traced, TraceKind } from '@imqueue/opentelemetry-instrumentation-imqueue';

class Reports {
    @traced()
    public async rebuild(day: string): Promise<void> {
        // span stays open until this promise settles
    }

    @traced({ kind: TraceKind.CLIENT, tags: { 'peer.service': 'billing' } })
    public async fetchInvoices(userId: string): Promise<Invoice[]> {
        return this.http.get(`/invoices/${ userId }`);
    }
}
```

