---
title: "traceStart() function · @imqueue/opentelemetry-instrumentation-imqueue"
description: "Starts a named span for tracing a block of code that no decorator can wrap, to be closed later by traceEnd() with the same name."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/opentelemetry-instrumentation-imqueue","url":"/api/opentelemetry-instrumentation-imqueue/latest/"},{"name":"traceStart","url":"/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.tracestart/"}]
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# traceStart() function

Starts a named span for tracing a block of code that no decorator can wrap, to be closed later by [traceEnd()](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.traceend/) with the same name.

**Signature:**

```typescript
export declare function traceStart(name: string, tags?: TraceAttributes, tracerName?: string): void;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

name


</td><td>

string


</td><td>

span name, and the key [traceEnd()](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.traceend/) will close it by


</td></tr>
<tr><td>

tags


</td><td>

[TraceAttributes](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.traceattributes/)


</td><td>

_(Optional)_ attributes to set on the span at creation; values must be strings


</td></tr>
<tr><td>

tracerName


</td><td>

string


</td><td>

_(Optional)_ tracer to create the span with, `'basic'` by default


</td></tr>
</tbody></table>

**Returns:**

void

## Exceptions

TypeError if a span under this name is already open

## Remarks

Spans started here are held in a module-level registry keyed by `name`<!-- -->, which is what lets [traceEnd()](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.traceend/) find one from an unrelated call site. Two consequences follow, and both matter:

- A name may have only ONE span open at a time. Starting a second under a live name throws rather than silently replacing it, since replacing would leak the first span forever. - A span left unclosed is never exported. Prefer [traced()](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.traced/)<!-- -->, or a `try`<!-- -->/`finally` around the [traceEnd()](/api/opentelemetry-instrumentation-imqueue/latest/opentelemetry-instrumentation-imqueue.traceend/) call, wherever the block can throw.

The span is created standalone, NOT as a child of whatever span is currently active, and it is not made active for the code in between. Use it to time a region, not to parent the spans that region creates.

## Example


```typescript
import {
    traceStart,
    traceEnd,
} from '@imqueue/opentelemetry-instrumentation-imqueue';

traceStart('import-batch', { 'batch.size': String(rows.length) });

try {
    await importRows(rows);
} finally {
    traceEnd('import-batch');
}
```

