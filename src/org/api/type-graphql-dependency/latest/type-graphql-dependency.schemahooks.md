---
title: "schemaHooks variable · @imqueue/type-graphql-dependency"
description: "Every deferred wiring hook registered so far, in the order it was registered."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/type-graphql-dependency","url":"/api/type-graphql-dependency/latest/"},{"name":"schemaHooks","url":"/api/type-graphql-dependency/latest/type-graphql-dependency.schemahooks/"}]
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# schemaHooks variable

Every deferred wiring hook registered so far, in the order it was registered.

**Signature:**

```typescript
schemaHooks: CreateSchemaHook[]
```

## Remarks

Applying [DependencyFor()](/api/type-graphql-dependency/latest/type-graphql-dependency.dependencyfor/) appends to this array; nothing in this package ever drains it. Running the hooks is the application's job, once the schema is built:

```typescript
const schema = await buildSchema({ resolvers: [...] });

schemaHooks.forEach(hook => hook(schema));
```
Skip that and no dependency is registered at all. There is no warning — the decorated classes look wired and their dependency fields stay empty.

