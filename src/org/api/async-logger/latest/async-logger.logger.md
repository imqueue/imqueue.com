---
title: "Logger class · @imqueue/async-logger"
description: "Logger that writes to the console without blocking the caller and forwards the same records to any configured winston transports."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/async-logger","url":"/api/async-logger/latest/"},{"name":"Logger","url":"/api/async-logger/latest/async-logger.logger/"}]
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# Logger class

Logger that writes to the console without blocking the caller and forwards the same records to any configured winston transports.

**Signature:**

```typescript
export declare class Logger implements ILogger 
```
**Implements:** [ILogger](/api/async-logger/latest/async-logger.ilogger/)

## Remarks

Implements [ILogger](/api/async-logger/latest/async-logger.ilogger/)<!-- -->, so it drops into anything in `@imqueue` that accepts a logger — `IMQClient`<!-- -->, `IMQService`<!-- -->, `RedisCache` — replacing the default synchronous console.

Console output is scheduled with `setTimeout` rather than written inline. That is the point of the package: a service logging heavily does not pay for it on the request path. It also means ordering against synchronous code is not guaranteed, and output queued at the moment of `process.exit()` is lost.

Transports are optional. Constructed with none — the default when `LOGGER_TRANSPORTS` is unset — this behaves as an async console logger and never touches winston.

## Example


```typescript
import { Logger } from '@imqueue/async-logger';

// configured from LOGGER_TRANSPORTS / LOGGER_METADATA
const logger = new Logger();

// or explicitly, e.g. to tag one subsystem differently
const audit = new Logger({
    transports: [{ type: 'file', options: { filename: 'audit.log' }, enabled: true }],
    metadata: { subsystem: 'audit' },
});

audit.info('user %s signed in', userId);
```

## Constructors

<table><thead><tr><th>

Constructor


</th><th>

Modifiers


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[(constructor)(options)](/api/async-logger/latest/async-logger.logger._constructor_/)


</td><td>


</td><td>

Constructs a new instance of the `Logger` class


</td></tr>
</tbody></table>

## Methods

<table><thead><tr><th>

Method


</th><th>

Modifiers


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

[error(args)](/api/async-logger/latest/async-logger.logger.error/)


</td><td>


</td><td>

Logs at ERROR level.


</td></tr>
<tr><td>

[info(args)](/api/async-logger/latest/async-logger.logger.info/)


</td><td>


</td><td>

Logs at INFO level — routine progress worth keeping.


</td></tr>
<tr><td>

[log(args)](/api/async-logger/latest/async-logger.logger.log/)


</td><td>


</td><td>

Logs to the console and to every configured transport.


</td></tr>
<tr><td>

[warn(args)](/api/async-logger/latest/async-logger.logger.warn/)


</td><td>


</td><td>

Logs at WARN level — something recovered from, but worth seeing.


</td></tr>
</tbody></table>

