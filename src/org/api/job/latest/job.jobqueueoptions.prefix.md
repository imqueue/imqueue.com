---
title: "JobQueueOptions.prefix property · @imqueue/job"
description: "Prefix for every key this queue creates in the broker."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/job","url":"/api/job/latest/"},{"name":"JobQueueOptions","url":"/api/job/latest/job.jobqueueoptions/"},{"name":"prefix","url":"/api/job/latest/job.jobqueueoptions.prefix/"}]
sitemap: false
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# JobQueueOptions.prefix property

Prefix for every key this queue creates in the broker.

**Signature:**

```typescript
prefix?: string;
```

## Default Value

"imq-job"

## Remarks

It namespaces the queue, so two queues sharing a name but not a prefix stay separate — which is how one Redis instance serves several environments. `@imqueue/core`<!-- -->'s default is `imq`<!-- -->; this package uses `imq-job` so job keys are distinguishable from RPC ones at a glance.

