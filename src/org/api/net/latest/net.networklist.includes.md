---
title: "NetworkList.includes() method · @imqueue/net"
description: "Whether an address falls inside any network in this list."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/net","url":"/api/net/latest/"},{"name":"NetworkList","url":"/api/net/latest/net.networklist/"},{"name":"includes","url":"/api/net/latest/net.networklist.includes/"}]
sitemap: false
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# NetworkList.includes() method

Whether an address falls inside any network in this list.

**Signature:**

```typescript
includes(ip: string): boolean;
```

## Parameters

<table><thead><tr><th>

Parameter


</th><th>

Type


</th><th>

Description


</th></tr></thead>
<tbody><tr><td>

ip


</td><td>

string


</td><td>

a bare address, with no `/prefix`


</td></tr>
</tbody></table>

**Returns:**

boolean

`true` if some network contains it.

## Exceptions

TypeError if `ip` is not a valid address — an empty string included. Screen untrusted input with [isValid()](/api/net/latest/net.isvalid/) first.

## Remarks

Binary search over the sorted, disjoint records, so O(log n) in the number of networks. Disjointness is not incidental — [toBinaryList()](/api/net/latest/net.tobinarylist/) coalesces overlaps precisely so that discarding one half of the list at each probe can never discard a record that covers the address. An address of the other family returns `false` rather than throwing, which is what lets [Networks](/api/net/latest/net.networks/) ask both of its lists without checking first.

Ranges are inclusive at both ends, so a `/32` matches exactly its one address.

