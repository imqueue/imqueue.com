---
title: "IPV4_MASKS variable · @imqueue/net"
description: "The 33 IPv4 network masks, indexed by prefix length — IPV4_MASKS[24] is the mask for a /24."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/net","url":"/api/net/latest/"},{"name":"IPV4_MASKS","url":"/api/net/latest/net.ipv4_masks/"}]
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# IPV4\_MASKS variable

The 33 IPv4 network masks, indexed by prefix length — `IPV4_MASKS[24]` is the mask for a `/24`<!-- -->.

**Signature:**

```typescript
IPV4_MASKS: bigint[]
```

## Remarks

Precomputed rather than shifted per call, because a conversion does this on every record in a list. Index 0 is `0n` (a `/0`<!-- -->, matching everything) and index 32 is all ones (a single host), so the array is 33 entries and indexing it with a prefix length needs no adjustment.

Exported as a plain array, so it is mutable — treat it as read-only. Reach for [masksOf()](/api/net/latest/net.masksof/) instead when the family is only known at run time.

