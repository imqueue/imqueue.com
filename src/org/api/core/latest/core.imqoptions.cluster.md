---
title: "IMQOptions.cluster property · @imqueue/core"
description: "Redis servers to spread this queue across. Supplying this — or IMQOptions.clusterManagers — makes IMQ.create() return a ClusteredRedisQueue."
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/core","url":"/api/core/latest/"},{"name":"IMQOptions","url":"/api/core/latest/core.imqoptions/"},{"name":"cluster","url":"/api/core/latest/core.imqoptions.cluster/"}]
sitemap: false
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# IMQOptions.cluster property

Redis servers to spread this queue across. Supplying this — or [IMQOptions.clusterManagers](/api/core/latest/core.imqoptions.clustermanagers/) — makes [IMQ.create()](/api/core/latest/core.imq.create/) return a [ClusteredRedisQueue](/api/core/latest/core.clusteredredisqueue/)<!-- -->.

**Signature:**

```typescript
cluster?: IMessageQueueConnection[];
```

## Remarks

Each entry carries its own host, port and optional `id` used for cluster-membership matching. Sends are distributed across the servers by health-aware round-robin, preferring ones whose connection is ready.

An entry may carry its own `tls`<!-- -->, overriding the top-level one for that server alone — for a cluster whose members do not share trust anchors. An entry that leaves it out falls back to the top level.

Per-entry `username` and `password` remain ignored: the underlying queues authenticate with the top-level credentials.

