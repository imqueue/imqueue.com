---
title: "RedisQueue.available property · @imqueue/core"
description: "Returns false only when this queue is known to be unable to accept writes right now — i.e., it has a writer connection currently in a non-ready…"
apiCrumbs: [{"name":"API reference","url":"/api/"},{"name":"@imqueue/core","url":"/api/core/latest/"},{"name":"RedisQueue","url":"/api/core/latest/core.redisqueue/"},{"name":"available","url":"/api/core/latest/core.redisqueue.available/"}]
sitemap: false
---

<!-- Do not edit this file. It is automatically generated by API Documenter. -->

# RedisQueue.available property

Returns false only when this queue is known to be unable to accept writes right now — i.e., it has a writer connection currently in a non-ready (reconnecting/closed) state. A queue that has not yet connected is considered available, since sending connects it lazily. Used for health-aware routing in the clustered queue.

**Signature:**

```typescript
get available(): boolean;
```

## Remarks

The writer connection is shared per `host:port` and TLS configuration within the process, so this reflects the health of that server connection rather than of this instance alone — every queue pointing at the same server the same way reports the same value.

