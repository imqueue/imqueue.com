# Hosts whose failures under `check:links:external` are expected, and therefore must
# not fail the weekly link-rot job. Consumed by --allow-external= (see
# scripts/check-links.js for the matching rule: exact host, subdomain, or URL prefix).
#
# The point of this file is that a job which cries wolf gets muted. Everything listed
# here is a link that WORKS in a browser and fails a scripted HEAD, or a deliberate
# placeholder — not a link nobody wants to fix. Anything genuinely broken must fail.
#
# Keep it short, and put the reason on the line. An entry with no reason is an entry
# nobody can ever safely remove.

# 403s an unattended HEAD from a datacentre IP; every /package/ URL on the site is
# generated from src/_data/apiPackages.js and cannot be a typo.
www.npmjs.com
npmjs.com

# The GPL-3.0 licence URL, cited from every SoftwareSourceCode node. gnu.org
# intermittently times out on HEAD; the URL has not moved since 2007.
www.gnu.org

# example.com / example.org appear in code samples by RFC 2606 design.
example.com
example.org
cdn.example.com

# Verified by the first working run of `check:links:external` (2026-08-03). Each of
# these answers a browser and refuses an unattended HEAD, for a reason:

# The hosted MCP endpoint is POST-only by design — a HEAD/GET returning 405 is the
# server behaving correctly, and Track B's B6 proposes making that stricter still.
https://mcp.imqueue.org/mcp

# 400 on the bare URL: it is VS Code's MCP install redirect and requires query
# parameters. The page links it with them; the check strips nothing, but the host
# rejects a HEAD either way.
https://vscode.dev/redirect/mcp/install

# Unreachable from an unattended HEAD; loads in a browser. Cited from both privacy
# policies as Clarity's data-processing statement, so it cannot simply be dropped.
privacy.microsoft.com

# The OpenAI plugin directory listing for @imqueue. chatgpt.com answers a browser
# and returns 403 to every unattended request, HEAD or GET, from a datacentre IP;
# the listing id is copied from the directory itself and cannot be a typo.
chatgpt.com
